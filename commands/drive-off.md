---
description: Turn drive mode OFF — back to normal behavior
allowed-tools: Bash(rm:*)
---

!`rm -f "$HOME/.claude/drive-mode"`

Drive mode has just been switched OFF (the flag file was removed). Confirm to the user in one line: drive mode is off; /drive still applies the contract to a single task on demand, and /drive-on re-enables the standing mode.
